defmodule ElasticsearchElixirBulkProcessorTest do
  use ExUnit.Case
  doctest ElasticsearchElixirBulkProcessor

  test "greets the world" do
    assert ElasticsearchElixirBulkProcessor.hello() == :world
  end
end
